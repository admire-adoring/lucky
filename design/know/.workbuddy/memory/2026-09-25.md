# 2026-09-25

## knowledge_index.html 改为纯色版
- 文件：`design/know/knowledge_index.html`（原地重写）
- 变更：删除 aurora 弥散色团（.aurora-bg/.blob）、全部 backdrop-filter/blur/linear-gradient/半透明 rgba 背景；令牌层换成纯色（新增 --border/--border-strong/--accent-soft/--accent-soft-2/--accent-text/--shadow-sm|md），亮/暗两套同步改。
- 结构优化：hero 改为「标题+徽标+4 格统计（带分隔线）」；搜索区加入筛选 chip（原型仅切态）；笔记项改为 head（标题左、时间右）+ 摘要 + 标签；主题项右侧加数量徽标；收藏项加「打开 ›」；图谱改为 SVG 连线 + 百分比定位节点（等距六向）+ 图例。
- 功能全部保留：tab 切换、新建笔记、主题切换（localStorage）、笔记/主题弹窗。

## 挂载「导航-v5」导航到 knowledge_index.html（同上文件，第二次原地重写）
- 参考源：`design/index/导航-v5.html`（2911 行，整块导航 = 顶栏胶囊 + 超级菜单 + 圆盘/平铺列侧栏 + 命令面板 + 侧栏槽）。
- 新导航：顶栏 9 个一级模块（只有知识库可进，其余 alert 且不改 active/不换色/不重建环）；侧栏 240px 圆盘承载 6 个分区（笔记/主题/标签/图谱/收藏/归档，即原页签）；槽面板改为知识库语义（最近笔记 / 快捷 6 项 / KPI 5 项 / AI 对话）；⌘K 命令面板含模块 + 6 个分区直达。
- 三处偏差（skill `module-prototype-nav-mount` 规定）：① 点非当前分区只切内容，点当前分区或中心门才下钻；② 非本模块不切走；③ 页面大标题删除，只留「知识库 / 分区」面包屑。
- 内容区改为 v5 组件语言：4 张 KPI 卡（.card-grid）+ 搜索/筛选 section + 列表 section + 右侧轨（待整理 / 最近主题）；`.card`/`.section` 统一走 `--bg-subtle` + 实描边，`--main` 是白色画布。
- 令牌：整套换成 v5 纯色体系（含 --bg-hover/--border-card/--shadow-float/--ink-on-accent），域色实色四档。**浅色 --m-main 由 #14B8A6 加深为 #0D9488**（原色在浅底仅 2.34:1，达不到图形 3:1）。
- 验证：CSS 括号平衡、CSS 块内无 color-mix/backdrop-filter、var() 无未定义、`node --check` 通过；jsdom 冒烟测试 34/34 PASS（圆盘项数/切分区/下钻/返回/非本模块不变/ mega 挂在 topbar / 折叠 / 命令面板直达 / 弹窗 / 主题）；关键令牌对比度全部达标（文字 ≥4.5，图形 ≥3）。

## 两次信息架构讨论（未改文件）
- 菜单分层判据、知识库 6 分区建议稿、两个「笔记」的归属判据 → 已整理进 `.workbuddy/memory/MEMORY.md`（长期约定，后续改原型前先读）。
- 待用户确认是否落地：① 知识库分区改为 笔记/收件箱/主题/标签/图谱/归档，收藏降级为笔记内的视图；② 示例数据按"结论型 vs 过程型"分开；③「升级为永久笔记」按钮从知识库弹窗移到学习模块。

## 落地：知识库菜单重设计（`knowledge_index.html`，第三次原地修改）
- 分区 6 项重定为 **笔记 / 收件箱 / 主题 / 标签 / 图谱 / 归档**（顺序按使用频率×任务类型；性质只留"内容归属"与"流程状态"，图谱是唯一的关系视图例外）。
- 收藏降级为「笔记」内视图：新增 `chips(data-view)` + 4 个 `.note-view`（all/recent/star/orphan），收藏 3 条数据搬入 star 视图；删除死 CSS `.favorite-*`。圆盘三级子项 = 视图，`syncNoteView()` 让环与 chips 双向同步。
- 新增「收件箱」面板（5 条待整理 + 行内动作 归类到主题/退回来源 + 整理规则三问）；右侧轨「待整理」改为「未链接的笔记 12 条」（去掉与收件箱的重复）。
- 笔记弹窗动作「升级为永久笔记」→「归档」；⌘K 增加「笔记视图」组（收藏仍可达）；快捷按钮「收藏当前」→「归类收件箱」。
- 校验：CSS 括号平衡 / CSS 内无 color-mix / var() 无未定义 / node --check 通过；jsdom 36/36 PASS（含分区顺序、视图切换、收件箱、环↔chip 同步、面包屑、mega 与 ⌘K 同步、弹窗动作）；圆盘几何未变（n=6，无需重算）。
- 顺手给技能 `module-prototype-nav-mount` 增加了「分区怎么定」一节（5 判据 + 视图降级必须搬数据 + 环与 chip 同步）。

## 讨论：笔记三层（记录/加工/生产）在知识库怎么体现（未改文件）
- 结论：三层不做分区，做**成熟度状态**，由「在不在收件箱 / 有没有双链 / 是不是永久」三个布尔量推导；三个症状因此变成三个可数断裂点（漏 5 / 懵 12 / 调不出来）。
- 方法论（康奈尔、卡片盒、常青笔记、PARA、子弹笔记）只映射到机制，不进菜单。
- 待确认落地补丁：笔记行成熟度徽标 + KPI 换三断裂点 + 详情弹窗显示"差什么才进下一层"。细节已写入 `MEMORY.md`。

## 落地：笔记三层 = 成熟度状态（`knowledge_index.html`，第四次原地修改）
- 新增 `.stage` 徽标（记录/加工/生产）+ 令牌 `--warn-soft/ink/line`（暖色表示未走完；浅色 5.18:1，深色 9.1:1）；由 `decorateStages()` 在初始化时按 `NOTE_STAGE` 表派发，**不新增数据字段**；归档行不标（已冻结）；收件箱行由 JS 绑定点开。
- KPI 四卡换成三个断裂点：`漏·记录层 5 / 懵·加工层 12 / 调不出来·生产层 274/286 / 永久笔记 42`。
- 笔记详情弹窗首段改为「成熟度 + 差什么才进下一层」（如"差 1 条入链才进生产层：建议链到「前端工程化」主题"），并为收件箱 5 条补齐弹窗数据（记录层 → 缺归类/缺结论/缺提炼）。
- 修掉一个真 bug：收件箱行内按钮点击会冒泡到行 → 同时弹窗，已加 `event.stopPropagation()`。
- 校验：jsdom 52/52 PASS（含徽标推导、KPI 数值、弹窗成熟度段、stopPropagation）；结构自检与 `node --check` 通过。

## 落地：圆盘三级菜单做实（`knowledge_index.html`，第五次原地修改）
- **收件箱三级**从「未归类 / 剪藏 / 速记」（点了内容不变，是假交互）改成「**全部 / 未归类 / 待补全 / 待提炼 / 待判断**」，各带计数；切换靠新增的 `data-status` + `row.hidden` 过滤，**不复制行**（`setInboxView()`），计数文案随过滤更新（如"待补全 2 条 · 未参与检索"）。
- **主题去掉三级**：它自身就是容器、点主题项已打开详情，不再承诺空的下一层（小圆点随之消失）。
- 函数 `syncNoteView()` 泛化为 `syncSectionView()`（notes → setNoteView，inbox → setInboxView）；`setSection()` 同时复位两类视图。
- 副作用修正：收件箱第 5 条标题「读书摘录（未归类）」与新的「未归类」视图重名 → 改为「读书摘录 · 反脆弱」（弹窗数据的 key 同步改）。
- 校验：jsdom **69/69 PASS**（含三级标签与项数、data-has-children 真值、过滤条数 5/2/1、计数文案、三级面包屑、返回后复位）。
- 技能 `module-prototype-nav-mount` 增补「三级要么真、要么不承诺」与「列表过滤别复制行（data-* + hidden）」两条。

## 修复：圆盘 6 个分区图标全都一样（`knowledge_index.html`）
- 根因：重写 `knowledgeSections` 数组时**丢了每一项的 `icon` 字段** → `iconSvg(undefined)` 静默回退到 `icons.grid`，6 个分区全变成同一个图形（页面不报错，只能肉眼看出）。
- 修复：补上 `笔记 pen / 收件箱 inbox / 主题 folder / 标签 tag / 图谱 graph / 归档 archive`（按"轮廓互不相撞"取值）；`iconSvg()` 对未定义名加 `console.warn` 兜底提示。
- 校验：jsdom **76/76 PASS**，其中新增断言——圆盘 6 个 SVG 去重后仍为 6、无一落在 grid 兜底、收起态 rail 与顶栏 mega 的三处图标集合同样互不相同、无 `[icon]` 告警。
- 技能补第 3 条防线：重写 sections 数组时 `icon` 最易丢，需 icon 字段 + 运行时告警 + 「按数量去重」的自检断言三件套。

## 产出：知识库功能说明书 + 修掉 KPI 双口径
- 新增交付物 `design/know/knowledge_features.html`（1043 行，单文件、零依赖、与原型同一套纯色令牌 + 亮/暗切换）：6 章 30 个功能条目，每条给「作用 / 适用场景 / 操作步骤 / 注意事项」四格，并标注三档状态（已实现 / 仅界面 / 演示提示）；含占位清单表、数字口径表、维护指引表（改分区/面板/徽标各改哪里 + 连带检查）。左侧粘性目录，所有锚点校验通过。
- 写文档时发现的真实不一致：侧栏槽 KPI 还是旧口径（笔记总数/本周新增/待整理/双向链接），而主区 KPI 卡已换成三个断裂点 → **已统一**为「漏 5 / 懵 12 / 调得出来 274÷286 / 永久 42 / 主题·标签 42·96」，并写进文档的"注意事项"（两组必须同口径）。jsdom 断言：前三项数字与主区一致 PASS。
- 校验：CSS 括号平衡、CSS 内无 color-mix/backdrop-filter/linear-gradient、目录锚点全部命中、6 章 27 条目编号连续覆盖 1–30、`node --check` 通过。

## 用户问「你觉得合理对吧，合理我就按你的来」
- 给了诚实的分层结论（A 判据推导 / B 我的任意选择 / C 必须用户输入的未知项）→ 已写入 `MEMORY.md`「置信度分层」。
- 明确不建议在 C 类未定前按"最终形态"实现；下一步应先把**数据模型 + 分流规则**写成一页纸验证，而不是继续加界面。
- 用选择题向用户收 3 个输入：双链习惯 / 真实笔记量级 / 下一步做什么。

## 用户回答 → 产出数据模型（`design/know/knowledge_model.html`，511 行）
- 用户答复：**双链会用且是核心方式** / **笔记量级上千条** / **下一步先写数据模型 + 分流规则**。
- 按这两个前提重排了结论：检索升为主入口、图谱只能画局部、成熟度必须自动推导 + 批量、归档必须有自动化出口；作废"主题 42 平铺""标签点一下就筛"两个早期假设。
- 产出 8 章草案：输入与硬约束 → 7 个实体字段表 → **8 个关键决策（含判据与反面后果）** → 状态机与分流规则（含状态推导 SQL、迁移动作表、三个断裂点口径、双链三件自动化、收件箱自动分流）→ 上千条下的检索与性能（查询模式上限、中文 trigram 索引、冗余一致性）→ 迁移与治理 → 明确不做 → **原型要跟着改的 6 处清单**。
- 建议的第一件实现（写在文档末尾）：只做一条纵向切片「捕获 → 收件箱 → 归类到主题 → 笔记列表（分页）→ 补一条双链 → 入链计数与状态跟着变」，跑通 4 个实体与两个冗余重算。
- 校验：CSS 括号平衡、无 color-mix/backdrop/gradient、目录 8 个锚点全命中、8 章 8 决策 17 张表、标签全配对、`node --check` 通过。

## 重构：圆盘下方栏（`knowledge_index.html`，第七次原地修改）
- 用户问「圆盘菜单下方栏应该有什么内容」→ 给出判据（这块只放"决定去哪个分区之前就有用"的东西：该做的事 / 刚做过的事 / 一行动作；不放资产统计）+ 发现**重复**：KPI 已在四处出现（圆盘计数、侧栏头部、KPI 槽、主区四卡）。
- 落地：删掉 tab 切换（`.slot-tabs/.slot-tab/.slot-panel/.slot-body`）、KPI 槽、AI 槽与它们的死 CSS；改为三块堆叠 —— **待处理**（收件箱 5 / 未链接 12 / 归档候选 37，`.todo-item[data-goto]` 可点直达）、**最近打开**（4 条，保留原 onclick）、**一行动作**（新建/导入/回顾，`.action-btn`）。矮窗口规则：≤860px 隐藏第 4 条最近项，≤760px 整块隐藏。
- 顺带补齐第三个队列的落点：`归档` 分区新增「归档候选」区块（24+8+5=37），把"规则只产候选、不自动执行"写进界面（对应数据模型决策 6）。
- 同步更新 `knowledge_features.html`：features 11–14 整条重写 + 目录条目改名 + 4 处过时引用（AI 槽、侧栏槽、两处"KPI 两处同口径"改为"四处同口径"）。
- 校验：jsdom **19/19 PASS**（旧 tab 已移除、三块结构与计数、三个计数各自可达、归档候选口径 24+8+5=37、最近项仍能开详情、收起态隐藏）；结构自检三份文件全通过（行数 1049 / 3326 / 511）。

## 修正：下方栏改回 3 个 tab（用户要求「和之前的布局一样」）
- 用户明确要求保留原 tab 布局 → 恢复 `.slot-tabs/.slot-tab/.slot-body/.slot-panel` 机制，tab 数从 4 减到 **3：待处理（默认）/ 最近打开 / 快捷操作**；删掉上一轮的堆叠式 `.slot-block / .slot-actions / .action-btn` 与矮窗口隐藏规则（一次只显示一个面板，不需要）。
- 待处理面板：3 行计数（5 / 12 / 37）仍可点直达；最近打开：4 条；快捷操作：恢复 2×3 网格 6 个按钮（新建笔记/新建主题/归类收件箱/随机回顾/导入剪藏/AI 助手）。
- 校验：jsdom **15/15 PASS**（3 个 tab 名称、默认在待处理、切换互斥、6 个按钮图标互不相同、计数可达、最近项可开详情、收起隐藏）；`knowledge_features.html` 的 f11 已同步为三 tab 版（含"默认 tab 是待处理""三 tab 合计约 190px，加第四个会横向滚动"两条）。

## 改名：下方栏三个 tab → 今日 / 最近 / 快捷（用户要求）
- 原型：全局替换 待处理→今日、最近打开→最近、快捷操作→快捷（共 18 处，含注释，避免代码与界面不一致）；核对过 9 处「今日」全部落在这块或别模块原有条目上（「今日概览」/任务「今日」未被误改）。
- 说明书：逐条精确替换 9 处（**不用全局替换** —— 否则会把「4 条最近打开过的笔记」这句改坏），并补两条注意事项：① tab 上的「最近」= 浏览历史，与「最近编辑」视图不是一回事；② **「今日」是存量队列口径**（今天该面对的三类待办），不是"今天创建/到期"，若改真实日期口径这三行数字要一起换。标签变短后三 tab 合计约 145px（预算 218px）。
- 校验：jsdom 6/6 PASS（三标签、默认仍在第一个、切换与计数可达均不变）；CSS 括号平衡、无 color-mix/linear-gradient、`node --check` 通过。

## 圆盘子菜单：6 个分区里 5 个配三级、归档不配（用户问「那个需要有子菜单」）
判据三条（全过才配）：① 有一根"找东西前会先想的那一刀"且 **≥3 类**；② 点下去内容真的换；③ 每项可数。逐项判定后 **笔记 / 收件箱 / 主题 / 标签 / 图谱 配，归档（只有 2 类）不配**。
- **主题**（新增三级）：一度按"自身就是容器"删掉过，按 42~上百个的量级补回 —— 但三级放**分组**（全部 42 / 技术 17 / 产品与系统 12 / 生活 13），不放"某个主题"。示例从 6 条扩到 9 条（3 组 × 3 条），行上 `data-group` + `hidden` 过滤。顺带写明"只分一层"。
- **标签**（新增三级）：三级 = **治理状态**（全部 96 / 高频 23 / 沉睡 55 / 待合并 18，互斥且和为 96）；19 个标签逐个打 `data-state`。
- **图谱**（新增三级）：三级 = **看什么** —— 局部图 / 全库统计（6 个数字）/ 孤儿巡检 12 / 黑洞巡检 3。四种是**四份内容**，用 `.sub-view[data-view]` 换块（不是 hidden 过滤，与主题/标签两套实现分清）。孤儿与黑洞各列 3 条，**每条都能点开详情**（为此给 `openNote` 补了 4 条笔记数据：SQLite 全文索引选型 / 个人记账的账户结构 / 支付对账的三种口径 / 权限模型 RBAC 拆解 / React 19 服务端组件）。
- **归档不动**：只有 2 类（已归档 / 候选 37），并列堆叠更省一次点击；面板内明说"不足 3 类所以不做三级"。
- JS：`syncSectionView()` 从一串 if 改成 **APPLY 表**（加分区只补一笔）；`setSection()` 复位清单补三笔。
- ⚠️ **踩到一个真 bug**：`setSection()` 里统一写 `setGraphView('all')`，而图谱四个视图里**没有 `all`** ⇒ 进图谱分区时 4 个 `[data-view]` 全部失去 `.active`、**面板空白且页面不报错**。修法：`const v = COUNT[view] ? view : 'local'` 回退到默认项。jsdom 断言「默认视图 = 局部图」抓住了它。
- 文案与计数：三处 `section-count` 加上 id（`#topicCount/#tagCount/#graphCount`）并随过滤更新；示例只列一部分时给两个数（`技术组 17 个 · 示例 3 个`）。
- 校验：jsdom **47/47 PASS**（6 分区 `data-has-children` 真值 + rail 同步、主题 9→3→3→9、标签 19→6→5、图谱四种看法与默认项、巡检条目可开详情并写明命中原因、切分区复位、归档不下钻、笔记/下方栏/图标去重无回归）；结构自检（CSS 356/356 平衡、`var()` 无未定义、配置图标名全在 `icons` 表、`node --check`）通过。三份文件：index 3634 行 / features 1129 行 / model 511 行。
- 文档同步：`knowledge_features.html` 第 4 章章首新增「谁配有一层子菜单」判据表（6 行判定 + 四条硬约束）；f17/f18/f19/f20 整条重写；第 6 章维护表补两行（加三级子菜单的三件套、分组计数要对账）。
- 技能 `module-prototype-nav-mount`：把「枚举 → chip、对象 → 三级」这条旧判据**作废**，换成按**用户行为**定的三条判据 + "三次踩坑各自修正了什么"的表 + 两种实现别混 + 复位值必须是合法 view key + 计数要对账 + 新增三级必加的 3 条断言。
