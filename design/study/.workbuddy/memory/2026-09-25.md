# 2026-09-25

## study_index.html 改为纯色风格
- 文件：`study_index.html`（原地覆盖）
- 移除弥散风格：删掉 `.aurora-bg` / 3 个 `.blob` 光斑、全部 `backdrop-filter` 毛玻璃、所有 rgba 半透明表面、`color-mix()` 取色。
- 令牌层重建为纯色：light（page #F4F5F7 / surface #FFF / border #E3E6EB / accent #9333EA + accent-soft #F5EFFE）与 dark（page #0E1014 / surface #171A20 / accent #B978F8），仅保留阴影中的 rgba。
- 结构与功能不变：8 个 tab、全部面板、7 个 JS 函数（openModal / openResource / openNote / openReview / openPractice / openOutput / closeModal）、主题切换与 localStorage 均原样保留。
- 优化点：hero 改为 accent-soft 实色块 + 左侧 3px 强调边；指标改为 pill 组（.metric）；卡片头统一为 `.card-head`（标题 + 右侧 hint）；tab 改为实色分段控件；资源项加右侧百分比与进度条；复习项加右侧 chevron；列表项加右侧状态 chip（.doc-status）；技能行加等级标签（.skill-lv）；主区限宽 1180px。
- 文案中性化：4 处「弥散风格」→「设计系统」（资源/笔记/项目/输出），日期对齐为 2026-09-25。

## study_index.html 导航栏对齐「导航-v5」原型（第一步）
- 参考文件：`design/index/导航-v5.html`（顶栏一级 + 超级菜单 + 圆盘侧栏 + 命令面板）。备份在 `/tmp/study_index_before_nav.html`。
- 搬过来的：`.topbar`（logo + 9 个一级胶囊 + `.mega` 二级下拉 + 右侧 新建笔记/搜索/通知/主题/AI/头像）、`.sidebar`（head + 圆盘 `.radial`（中心门 + 分区刻度弧 + 罗盘指针 + 悬停名称条）+ 收起态 `.rail` + 四槽面板 最近/快捷/KPI/AI + 收起按钮）、⌘K 命令面板。
- 删除旧的 `.tabs/.tab` 页签（DOM + JS 监听全删），分区切换改由圆盘 / 图标列承担。
- 关键适配（与 v5 的三处偏差）：
  1. 圆盘二级 = 8 个内容分区，**点非当前项只切内容、不下钻**；下钻只由「点当前项」或中心门触发（否则一进目标/资源就丢掉 8 分区视图）。
  2. 只有 `learning` 模块可进；其余 8 个模块保留导航与二级 mega 清单，点击 alert 占位且不改 active。
  3. 面包屑只给上级：`学习 / 当前分区`，三级时 `学习 / 二级 / 子项`。
- JS 契约：`primaryConfig.learning.items[i].key === 面板 data-panel`（dashboard/goals/skills/resources/notes/review/practice/output）。
- 令牌策略：加了一层别名（`--bg-surface→--bg-card`、`--border→--border-subtle`、`--accent*→--m-*`），内容区 CSS 一行未改即完成换肤。域色实色化：浅色 main #D946EF / ink+fill #A21CAF / soft #FAE8FE / line #F0C8F8；深色 main=ink=fill #E879F9 / soft #2C1035。
- 校验：CSS 括号 312/312 平衡、`color-mix` 与 `backdrop-filter` 计数为 0、无未定义变量（仅 --a/--i/--arcs/--cols/--na 为内联写入）、`node --check` 通过、JS 引用的 20 个 id 全部存在。
- 新技能已落库：`~/.workbuddy/skills/module-prototype-nav-mount/`（下一步给生活/工作等模块挂同一套导航时直接用）。
- 待办（下一步）：其余模块原型（生活 / 工作 / 项目 / 知识库 / 设置）挂同一套导航。

## 学习模块「菜单分区」定稿（书架式自学）
- 用户已确认学习形态 = **书架式自学**（以兴趣/技术书/课程为主，无考试排期）。
- **定稿 8 个分区**（数量与现状相同，做 3 个动作）：
  1 概览（补「今日计划 + 本周时长」，计划不新开分区）/ 2 目标 / 3 技能（重定义为「能力地图」：缺口 → 目标的上游）/ 4 资料（原「资源」改名）/ 5 笔记 / 6 复习 / 7 练习（原「实践」改名）/ 8 输出。
- 二级清单：目标=进行中/已完成/已搁置（用状态而不是季度/年度）；资料=书/课程/文档/视频；笔记=永久/文献/闪卡；复习=今日到期/本周/全部；练习=题目/代码/写作；输出=博客/分享/开源；技能=技能树/缺口/自评；概览=今日/本周/AI 简报。
- 名词定义（写进文档，避免反复争）：学习=过程与材料；知识库=沉淀的成品；项目=有交付物的执行；「闪卡」卡片本体在笔记、到期排程在复习；练习与项目的判据 = **有没有交付物**。
- 待办：按此把 `study_index.html` 的 8 个面板与 `primaryConfig.learning.items` 一起改名/重定义（含圆盘 n=8 几何不变：R=57、icon=35、容器 169px）。
- 复用的判断标准（4 道闸门）：容器性（能反复进入看累积列表，一次性动作不占一级）/ 对象唯一（一个菜单只装一类实体）/ 状态独立（有别的菜单没有的状态字段，口径相同即重复）/ 单一归属（不同时属于两个模块）。概览是非容器（仪表），唯一合理例外。提案与变更清单见本轮对话。

## 修订：用户身份 = 程序员，学习形态是「新技术 + 背面试题」双轨
- 用户自述：程序员，要学新技术、背面试题，两者都算。→ 推翻上一节的「纯书架式自学」定稿。
- **面试内容按「能不能靠记忆回答」一分为二**（这是本轮的核心判据）：
  - 能背（八股 / 面经 / 概念题）→ 新对象「**题库**」，状态 = 掌握度 + 标签（技术栈 / 公司 / 难度）；技术栈与资料不同（资料有"进度%"，题有"掌握度"），故不合并。
  - 必须动手（算法 / 系统设计 / 白板）→ 归现有「**练习**」，状态 = 正确率 + 耗时。
  - 两边的「到期」都汇集到现有「**复习**」（间隔重复只实现一次，不重复造）。
- **不新增「计划」分区**：面试倒计时本质是「带期限的目标」，已在「目标」里（deadline 字段）；时间维度归属顶层「日程」。→ 程序员版 = **9 个分区**：概览/目标/技能/资料/题库/笔记/复习/练习/输出。
- 圆盘 n=9 几何：R=64、icon=35、容器 183px（≤218 上限），无需改 CSS 结构。
- 求职本身（简历 / 投递 / offer / 面试安排）是**有交付物的项目** → 归顶层「项目」模块；学习模块的题库只标注"关联项目"，面试季结束后可置为归档。
- 待办：把 `study_index.html` 落成 9 分区版（含新增「题库」面板与圆盘第 6 项）。

## 已落地：study_index.html = 9 分区版
- 一级分区顺序（= 闭环顺序，面板 DOM 顺序与之一致）：概览 / 目标 / 技能 / 资料 / 题库 / 笔记 / 复习 / 练习 / 输出。
- 二级清单：概览=今日·本周·AI 简报；目标=进行中·已完成·已搁置；技能=技能树·缺口·自评；资料=书·课程·文档·视频；题库=全部·薄弱·已掌握·按公司；笔记=永久·文献·闪卡·主题；复习=今日到期·本周·薄弱项；练习=题目·代码·写作；输出=博客·分享·开源。
- 新增内容：**题库面板**（复用 `.resource-item` 形状：题干 + 掌握度% + 进度条 + 技术栈/公司/难度三标签，6 道示例题，新增 `openQuestion()` 弹窗）；**概览新增「今日计划」**（可勾选，`.plan-check` 只改状态不落库）与**「本周时长」**（7 天条形 + 周合计）；**技能改为「能力地图」**并加 `.skill-gap` 缺口列（缺口按面试权重排序）；**目标新增秋招倒计时目标**（截止 10-20，40%，剩余 25 天）。
- 改名落地：资源→资料、实践→练习（面板内容换成算法/系统设计/白板，不再放有交付物的项目）。
- 同步更新的文案位（配置带不上的地方）：hero 指标、第三张统计卡换成「题库掌握 68%」、槽面板 快捷(添加资料/录入题目/能力地图) 与 KPI(题库掌握·薄弱题·在学资料)、AI 三条话术、最近访问加一条题目。
- 校验：CSS 括号 331/331；`color-mix` = 0；无未定义变量（仅 --a/--i/--arcs/--cols/--na）；一级 items 与 `.panel[data-panel]` 顺序集合完全相等（9/9）；`node --check` 通过；onclick 引用的函数全部有定义。
- 圆盘 n=9 几何由 JS 自动推导：R=64、icon=35、容器 183px（≤218 上限），CSS 未改。
- 技能更新：`module-prototype-nav-mount` 增加「改分区数量的固定清单」（6 步 + 校验式 + n=8/9/10/12 的几何取值表）。

## 约定：练习写出来的东西放哪（待用户确认）
- 判据一：**需要 git 仓库吗**（多文件 / 会继续改 / 想留下）→ 需要 = 升级为「项目」对象；只是当次片段 = 留在「练习」当次记录里当附件（不建对象）。
- 判据二：**有人等它吗**（验收方 / 上线日期 / 使用者）→ 有 = 顶层「项目」的工作/生活域；没有 = 顶层「项目」的学习域（练手）。
- 归属规则：**代码本体只存一处（项目）**，「练习」只留「关联项目 + 复盘」，不复制正文 —— 符合单一归属闸门。
- 清理规则：学到的摘进笔记后，练手项目置「归档」；判据 = 该项目还能不能生产价值（复用到真实工作 / 别人用）。
- 刷题答案（单文件、无仓库）不算项目，留「练习」。
- 待落地（未做）：练习面板每行加"产物 → 项目"入口；顶层 projects 配置加「来源：练手 / 真实需求」筛选项。不新增分区。

## 已落地：学习模块里的项目 = 挂项目模块的同一份 UI（跨模块共享视图）
- 用户明确：**学习模块的项目只是加入了项目的 UI** —— 项目对象与字段（域 / 来源 / 进度 / 里程碑 / 期限 / 仓库）全部归「项目」模块，学习模块只挂壳，不复制列表、不复制正文。
- `study_index.html` 落法（对应 React 版三件套）：
  - 数据源 `PROJECTS`（3 条：跟教程写的 Todo App / 仿写组件库 / 客户门户改版，含 domain + source）；
  - 唯一渲染 `renderProjectList(el, filter)` —— **壳传 filter**（等价于 hook 的 basePath）；
  - 唯一详情 `openProject(id)` —— 弹窗里同时给出"学习侧的关联（不属于项目对象）"和"归档判据"；
  - 挂载点 `<div class="proj-list" data-mount="practice">`，init 时喂 `p => p.domain === '学习'`（所以 工作域的客户门户改版 在学习模块里看不到，证明 filter 真的生效）。
- 练习面板新增「关联项目」卡，右上角挂 `.shared-badge`「共享视图 · 项目模块的同一份 UI」——让评审者看出这块不是学习模块自有的，避免有人提"这里再加个字段"。
- 「练习」原有的三行（LeetCode 75 / 系统设计短链 / 白板 Promise.all）仍是**练习记录**，不是项目；不新增分区。
- 校验：CSS 括号 343/343；9 个面板不变；`data-mount` 命中 1 处；无未定义变量；onclick 引用函数全部有定义；`node --check` 通过。
- 技能更新：`cross-module-shared-view` 增加「静态单文件原型的等价做法」对照表 + 两条纪律（第二壳不存在时也要写成收 filter 的形式；共享块要挂可见标记）。

## 更正：项目独立成第 10 个分区
- 用户追问「所以你加这个菜单了吗」→ 之前把共享视图塞在「练习」面板底部，**违反了我自己定的「状态字段不同不该同区」**（练习=正确率/耗时，项目=里程碑/期限/验收方）。已改为独立分区。
- 最终 10 个分区：概览 / 目标 / 技能 / 资料 / 题库 / 笔记 / 复习 / 练习 / **项目** / 输出（项目排在练习之后、输出之前）。
- 「项目」分区二级：进行中 / 已完成 / 已归档（沿用"用状态不用时间"的规则）。
- 分区内容两张卡：①「学习域的项目」= 共享视图挂载点 `data-mount="projects"`（filter `domain === '学习'`）+ `.shared-badge`；②「归属与清理」= 四条规则（对象归项目模块 / 只读视图不新增字段 / 练习记录升级判据=需不需要 git 仓库 / 归档判据=还能源出价值）。
- 「练习」面板底部的关联项目卡已删除（同一模块内不做两处挂载），改为 card-hint 一句交叉引用；练习父项图标从 project 换成 code，project 让给「项目」。
- 校验：10 个面板与 items 顺序一致；圆盘 n=10 → R=71 / icon=35 / 容器 197px（≤218 上限，CSS 未改）；CSS 括号 343/343；无未定义变量；onclick 函数全有定义；`node --check` 通过。

## 修掉两处真实重叠（题库⇄练习、笔记⇄题库）
- 判据一（题库 vs 练习）：**最小单位能不能「翻面自评」** —— 能（问→答）= 卡片进题库；不能（产物是代码/图，要跑要评审）= 练习记录。LeetCode 归练习（产物是代码），八股归题库。一次练习可关联多张卡（多对多，不算重复）。
- 判据二（笔记 vs 资料）：**有没有「外部的、能被读完的原始件」** —— 有 = 资料（状态是读到哪、有终态）；没有 = 笔记（无进度，只有成熟度）。剪藏必须拆两条：原件进资料 + 我的批注进笔记，用 source 互相指。
- 管道（四层）：资料（外部原件）→ 笔记（我的加工）→ 知识库（成品，归知识库模块）→ 输出（对外发布）。
- 卡片只有一个家：**题库**。`笔记` 的二级删掉「闪卡」，改成熟度递进 速记 / 文献 / 永久 / 主题；题库二级改为 全部 / 面试 / 笔记卡 / 薄弱 / 已掌握（按来源 + 状态），题库面板补一张来源=笔记卡的示例卡 + 「卡片池的归属」规则卡。
- 命名修正：`练习` 二级「题目」→「代码 / 白板 / 写作」（避免与「题库」撞名）；`资料` 二级补「文章」（剪藏原件落在这里），资料面板加「与笔记的边界」规则卡。
- 命名规则（新）：二级枚举里**实体词不许撞名**，**状态词允许跨分区重复**（目标/项目各有"进行中"，修饰的是各自对象）。
- 项目长期约定已落到 `study/.workbuddy/memory/MEMORY.md`（模块与对象归属表 + 5 条判据 + 4 条边界 + 命名规则 + 跨模块共享视图纪律）。
- 校验：10 个面板与 items 顺序一致；CSS 括号 343/343；无未定义变量；onclick 全有定义；`node --check` 通过。

## 把约定落到 UI（不只是写在注释/记忆里）
- **题库加筛选栏**：`.filters` + `.filter-chip`（新增这两个类，之前原型里的 `.tag-cloud/.tag-item` 在纯色化时已删掉，不要复用旧名）；chip 值 = 全部 / 面试 / 笔记卡 / 薄弱 < 60% / 已掌握 ≥ 85%，与题库二级完全对应。
- **筛选值全部从已渲染内容派生**（`applyQuestionFilter`）：来源 = 标签里有没有「笔记卡」，掌握度 = 读 `.resource-pct` 数字 —— **没有为筛选加任何 data-* 字段**，实现"同一格能算就别存"。阈值只在 JS 顶部写一次（`WEAK_BELOW = 60` / `MASTERED_AT = 85`），与「复习」的薄弱口径一致。
- **笔记加成熟度 chip**：`永久`（acc）/`文献`（mute）/`速记`（mute），新增 `.doc-status.mute`；meta 里补"源：XX 资料"体现与资料的单向引用；不再有统一的"笔记"chip。
- **复习加来源标记**：每条卡片标 `笔记卡 / 面试卡`；新增规则卡「为什么这里是调度器」（不存对象 / 只实现一套间隔重复 / 薄弱阈值 60% 两侧统一）。
- **练习加"关联卡片 N 张"**：体现"一次练习关联多张卡（多对多，不算重复）"。
- 校验：CSS 括号 348/348；10 分区/10 面板一致；题库 7 张卡（薄弱 4 / 已掌握 2，与示例数字自洽）；笔记 chip 4 个（残留"笔记"0 处）；复习来源标记 4 处；练习关联卡片 3 处；无未定义变量；`node --check` 通过。

## 澄清：题库 vs 练习的根判据（用户两次表示没理解，旧表述已弃用）
- 用户反馈「有没有产物」太绕。改用根判据：**题库 = 我知不知道；练习 = 我能不能做出来**（知道 ≠ 做得到）。
- 三条一秒可判的检验：① 需要打开编辑器/画板吗 ② 做不出时丢的是记忆还是技能 ③ 界面是工作台还是记录本。
- 边界表（面试准备场景）：TCP 三次握手 / React Fiber / 缓存穿透雪崩 / 一致性哈希 / 「讲项目最难的点」→ 题库；两数之和 / LRU / 手写 Promise.all / 防抖节流 / 设计短链演练 → 练习。
- 关键推论：**编程题的"套路"在题库（20 秒），"落地"在练习（20 分钟）**；题型套路可背，某题 AC 是练习记录；两者多对多关联。练习 ≠ 只有编程题（还有系统设计 / 白板口述 / 复盘写作）。
- `MEMORY.md` 的判据 1 已替换为上述版本（旧的"翻面自评"表述不再作为根判据）。
- 待办（可选）：把界面上题库面板「卡片池的归属」的第 1、3 条换成"知道 vs 做得到"+三条检验，并给练习面板补一张同款规则卡。

## 删除「输出」分区（10 → 9）
- 用户理由：**这是我个人维护的，我也不需要分享，别人有自己的学习方法**。
- 结论：按"状态独立"闸门，输出该删 —— 它的三个独有字段（渠道 / 发布日 / 反馈）在自用系统里一个都用不上。
- 内容去向：①"把学到的东西整理成讲得通的成品"→ **笔记的主题级**（成熟度顶端，新增示例「设计系统（主题整理）」）；②"讲一遍检验真懂"→ 题库的卡 + 练习的白板口述；③ 需要给人看的凭证（开源项目）→ 顶层「项目」模块。管道终点从"输出"改为**知识库**。
- 改动清单：删 `output` 配置项与 out-blog/out-share/out-os 二级、删 output 面板、删 `openOutput()` 函数；资料规则卡去掉"对外发布的那份归输出"；openProject 弹窗文案改为"摘进笔记（成熟度走到主题级）"；handleRingClick 注释里的分区清单同步；文件头注释从 8 面板更新为 9 面板；笔记分区新增说明卡「为什么这里没有「输出」」。
- 几何：n=10 → 9，圆盘自动重算 R=64 / icon=35 / 容器 183px。
- 校验：CSS 括号 348/348；9 分区与 9 面板顺序一致；`openOutput` 残留 0；onclick 引用函数全有定义；无未定义变量；`node --check` 通过。文件里剩余的"输出"字样全部是"解释为什么没有它"的文案。

## 重做侧栏「圆盘下方栏」的四个槽
- 发现真重复：原「KPI」槽的 6 个数字与「概览」页 hero + 统计卡是同一批（本周 8.5h / 待复习 / 题库掌握 / 薄弱 / 在学资料）⇒ 同一批数字出现两处。
- 新四槽（tab 与 panel 一一对应，默认 `today`）：今日 / 快捷 / 轨迹 / 冲刺。
  - **今日**（默认）：4 条可点入口 —— 待复习 5 条(→复习) / 今日计划 3·5(→概览) / 薄弱题 38(→题库+自动切到"薄弱"筛) / 继续 React 19 文档 45%(→资料)。
  - **快捷**：新建笔记 / 录入题目 / 添加资料 / 记一轮 / 复习卡 / 能力地图（**去掉 AI 按钮**，顶栏已有常驻入口）。
  - **轨迹**：时间序 + 跨分区 5 条（背卡 / 刷题 / 改笔记 / 读完资料 / 录入题），带相对时间。
  - **冲刺**：`25 天后 · 10-20 秋招面试` + 3 条进度（秋招目标 40% / 题库掌握 68% / 本周时长 8.5h），复用 `.hour-*` 渲染。
- 新增 `filterQuestions(kind)`：chip 与「今日 · 薄弱题」入口共用同一个函数，所以从哪边点进来高亮与结果都一致。
- 清理死规则：删掉 `.kpi-*`（6 条）与 `.ai-*`（12 条）共 18 行 CSS，原地留注释说明为何删、别再加回来；CSS 括号从 348 → 333。
- 校验：槽 tab 与 panel 完全对应且默认 today；旧槽 data-* 残留 0；死类 HTML 引用 0；槽内 12 个类名均仍有样式；无未定义变量；`node --check` 通过。

## 「冲刺」槽改为「进度」槽（去场景化）
- 用户提问：「冲刺放什么，如果我目前没有找工作的需求呢」。→ 「冲刺」这个名字预设了"有终点、在倒计时"，非求职期这个槽会永远空着。
- 改法：槽的语义改为**「还剩多久 + 走到哪」**；tab 名 冲刺 → **进度**（data-slot/panel 同步改 `progress`）。
- 新增 `renderProgressSlot()` + `DEADLINES` 只读数据（4 个带期限目标）+ `STREAK_DAYS`：
  - 有未过期的带期限目标 ⇒ 头条 =「剩余天数 + 最近截止的那个」；今天(2026-09-25) 算出 **5 天后 · React 19 工程化**（秋招 10-20 是 25 天，故自动让位给 09-30 —— 正好证明头条不绑求职）。
  - 一个都没有 ⇒ 退化为「12 天连续学习 · 本周 8.5h」。
  - 下面三行口径固定：目标进度（与头条同源，名称不重复）/ 本周时长 / 题库掌握。
- 剩余天数用**真实今天**计算（不再写死），所以这个数字不会随时间过期；其余面板日期仍是示例值。
- 同步更新：槽区顶部注释、死 CSS 注释里的"冲刺"字样、`MEMORY.md` 的槽表与新规则。
- 校验：槽 tab/panel = today/quick/trail/progress；`sprint` 残留 0、「冲刺」残留 0；进度槽挂点 id 齐全（progBig/progText/progBar/progPct）；`renderProgressSlot` 已定义且被调用；CSS 333/333；`node --check` 通过。

## 三级菜单交互落地（9 分区收敛到 2 个三级）
- 判据收紧：**三级 = 值得"记住位置"的地方**，不是视图切换；且子页之间要"列都不一样"。⇒ **枚举做 chip，对象做三级**。
- children 重写：**练习 = 7 个题单**（lc75/offer/hand/sql50/sd5/oral/retro）、**笔记 = 6 个主题**（frontend/cs/sd/ui/efficiency/inbox）；其余 7 个分区的 children 全部删除（概览/目标/技能/资料/题库/复习/项目）。
- 三个面板从静态列表改成**数据驱动 + 挂载点**：`#skillBody`（renderSkills：技能树按域分组 / 缺口按缺口排序）、`#setBody`（renderPracticeSets：题单卡片，进度由 done/total 算出，白板类换文案）、`#noteBody`（renderNotes：按主题筛，成熟度做 chip）。
- 交互契约：`applyChildView(parentKey, childKey)` → `CHILD_VIEW = { practice: selectPracticeSet, notes: selectNoteTopic }`。
  - 点三级项 → 真的切到该子视图（不再只改面包屑）；
  - 下钻（中心门 / 点当前项）→ 立即应用第一个子项；
  - 返回二级（中心门 / Esc / Backspace / 图标列返回）→ 应用 `'all'` 回到全集。
- 页内不重复：练习/笔记**不再放与三级同款的 chip**（避免两套入口做同一件事），只留虚线边的「+ 新建题单 / + 新建笔记 / + 新建主题」；技能因为不走圆盘，页内保留「技能树 / 缺口」切换。
- CSS 新增：`.filter-chip.add`（虚线，表示"新建"而非"筛选"）、`.skill-domain`（域小标题）。括号 336/336。
- 校验：9 分区 / 9 面板一致；练习三级 keys 与 PRACTICE_SETS keys **完全相等**（7/7）；笔记三级 keys ⊆ TOPIC_NAME、每个主题都 ≥1 条笔记；旧 children key 残留 0；三个挂载点均有渲染函数；`applyChildView` 被 4 处调用；无未定义变量；`node --check` 通过。

## 修正：「题单/主题」是分类不是子菜单（用户指出）
- 用户原话：「这也不算子菜单啊，这不就相当于分类吗」—— **成立**。判据从"枚举 vs 对象"换成更严的**"唯一归属 vs 多重归属"**：
  - 真子级 = 父对象的组成部分，只能有一个父级（书的章节、目标的关键结果、项目里程碑）→ 可以占一层；
  - 分类 = 同类对象的集合，可同时属于多个（题单、主题、标签、公司）→ chip / 选择器。
- 一秒检验：**同一个东西会不会出现在两个子页里**（《两数之和》同时在 LeetCode 75 与剑指 Offer）⇒ 分类。
- 结论：**圆盘只保留两级**（模块 → 分区），9 个分区平级；题单/主题改用页内顶部选择器；真子级放到**详情页内的目录/锚点**。
- 影响：上一轮刚做的"练习=题单、笔记=主题"两个三级**待撤回**（尚未执行；代码仍在文件里，`CHILD_VIEW` 目前登记 practice / notes）。`MEMORY.md` 的该条已改写为修正版。
- 教训（写给未来的自己）：判"要不要一层导航"时，先问"它是不是父对象的组成部分"，而不是"它是不是对象"。**对象 ≠ 子级** —— 集合也是对象。

## 执行撤回：圆盘回到两级（9 分区平级）
- `primaryConfig.learning` 的 children **全部删除**（笔记 6 个主题、练习 7 个题单也一起撤），9 个分区现在都没有三级。
- 题单 / 主题改为**页内顶部选择器**：练习面板加 `<select id="setPicker">`、笔记面板加 `<select id="topicPicker">`，选项由数据生成（新函数 `fillPicker(id, allLabel, items)`），样式新增 `.picker / .picker-row / .picker-label`。原「+ 新建题单 / 新建笔记」从底部挪到选择器同一行。
- `selectPracticeSet / selectNoteTopic` 增加**反向同步**：`sel.value = key`，保证选择器与列表永远一致（从任何入口进来都同步）。
- 删除 `CHILD_VIEW` / `applyChildView` 与三处调用；`enterLevel3 / backToLevel2 / 圆盘 is-nested / 图标列返回按钮` **保留**（v5 通用机制），并在注释里写明"当前没有分区在用，等真出现唯一归属的子级再接"。
- 在 `openResource()` 弹窗里加了一节**「目录 · 章节」**演示真子级该放哪：第 1 章已读完 / 第 2 章在读（45% 进度就是它算出来的）/ 第 3 章未开始。
- 同步修掉 4 处过期注释与 2 处 hint 文案（"在圆盘三级上切换" → "用上方选择器切换"）。
- 校验：分区 n=9、**剩余三级项 0**；`applyChildView`/`CHILD_VIEW` 残留 0；两个选择器 id 存在；onchange 引用的函数都有定义；CSS 340/340；无未定义变量；`node --check` 通过。





## 新区「学习 · 目标」分区独立原型页（`学习-目标-index.html`）
- 产出：`design/study/学习-目标-index.html`，3383 行独立单文件，双击即开。
- 做法＝复制模块主页只换内容区：`cp study_index.html`，外壳 / 令牌层 / JS 原样沿用；四处锚点：头注释与 title / CSS 在 `</style>` 前追加 / 内容区 9 面板换成「目标 + `data-panel="__other"` 占位」/ JS 在 `/* 初始化 */` 前插入；初始化 `currentItemIdx = 1`、`activatePanel('goals')`。
- **踩到的坑（已修）**：① 用脚本替换头注释后行号位移，末尾又被补了一行 title ⇒ 出现两个 `<title>`，删掉游离的那个；② `activatePanel` 的兜底是"显示第一个面板"，单分区页里会造成「面包屑写资料、内容显示目标」的假状态 —— 改成落到 `__other` 占位卡。
- 界面结构：hero（6 目标 · 进行中 4 · 最近截止 09-30）→ 4 格 KPI（最近截止 / 平均完成度 48% / 落后于时间 2 / 缺支撑 1）→ 工具栏（状态筛选 + 排序）→ 双列：主列 6 张目标卡（状态徽标 → 验收口径 → 完成度 vs 时间进度 → 关键结果 → 支撑来源 → 动作），右列 到期时间轴 / 对齐检查 / 状态分布 / 归属与口径。
- 口径落到界面的两处硬约束：① 目标状态不存字段，由 **期限 + 完成度** 派生；② 关键结果里标「由……推算」的格子（题库掌握度 / 练习记录 / 项目进度）不可手点 —— 手填的进度一定会撒谎。
- 新增令牌：`--warn-*/--danger-*/--info-*` 各 3 值（亮 + 暗），全部按 ≥4.5:1 选值（文字色对各自 soft 底）。
- 校验：jsdom 无头，30 项断言全过、无脚本错误 —— 9 胶囊 / 9 圆盘项 / 9 图标列；默认面板 goals、面包屑「学习 / 目标」；6 卡 / 4 KPI / 5 时间轴 / 4 对齐 / 18 关键结果；筛选 4·1·1·6 与计数联动；KR 可勾、推算格不可勾；点「资料」→ 占位卡、点回「目标」复位；详情弹窗 / 主题 / 侧栏折叠正常。标签配平 346/346；`node --check` 通过。
- 未做：`study_index.html` 里那版薄目标面板（4 条进度条）没有动；两页之间没有真实跳转，只有演示 alert。

## 目标页收敛（用户反馈「怎么看着这么乱这么多」）
- 诊断：不是"东西不够好"，是**同一件事说了三遍**。三处真重复：① 统计 —— hero 5 metric + 4 格 KPI + 右栏状态分布；② "还剩多久" —— 卡右侧期限 + 徽标「剩 25 天」+ 右栏垂直时间轴；③ 支撑来源 —— 卡上 chips + 右栏对齐检查卡。再加 6 张卡每张 3 个按钮 / 3 个徽标 / 一行"最近动作"。
- 收敛（原则：一屏同一件事只说一遍）：
  - 删 4 格 KPI 卡组（hero 里已有 5 个数）→ hero 保留一句结论 + 5 metric。
  - 删整个右栏（垂直时间轴 / 对齐检查 / 状态分布 / 归属与口径 4 张卡）→ 时间轴折成 **hero 下一条横向时间条**（4 节点：日期 + 名字 + 完成度·剩几天，点色 soon/over/中性）；对齐检查的判据由卡上「缺支撑」徽标承担；归属口径压成**页脚一行**。双列 → 单列。
  - 卡瘦身：徽标每卡 ≤2（只在有话说时加警示：1 张"落后 29pt"红、1 张"缺支撑"黄，其余只有"进行中"）→ 徽标总数 11 → 8；删「最近动作」行；验收口径从灰底块改为左侧 2px 细竖线一行；KR 行距 6→4px；按钮 3 → 1 主 + 1「···」（次要动作收进弹窗）。
  - 已完成 / 已搁置改用**两行紧凑卡**（标题 + 100%/20% + 一句口径 + 1 按钮），并把**默认筛选从"全部"改为"进行中"** ⇒ 首屏只有 4 张卡。
- 结果：文件 3383 → 3094 行（175KB → 157KB）；默认可见块 = hero + 时间条 + 工具栏 + 4 张卡 + 页脚 = 8 块；删掉的类（`.goal-layout/.goal-col/.tl-item/.align-item/.dist-row/.grid-4/.goal-log/.g-link-label`）残留 0；`--info-*` 三值已随之删除（无死令牌）。
- 校验：jsdom 40 项断言，仅"徽标总数"一项是我预期写错（实际 6 个标准卡徽标 + 2 个紧凑卡 = 8，非 5），页面本身无误；标签配平全 OK；CSS 花括号 782/782；`node --check` 通过；无脚本错误。

## 目标页补上「新增 / 维护」（用户问「那目标怎么新增，怎么维护呢」）
- 设计前提：**派生字段一律不进表单**。完成度 = 关键结果的达成比例（来源选 题库掌握度 / 练习记录 / 项目进度 的格子自动更新），状态由 期限 + 完成度 派生 ⇒ 表单里只有 目标 / 期限 / 验收口径 / 关键结果（各带来源）/ 支撑来源。表单里没有的东西，人就不会去维护它。
- 三个入口，都不增加常驻版面：
  - **新增**：工具栏右侧「+ 新建目标」→ 弹出对象字段表单（`.gform` 一套表单件，新建与编辑共用一份 `openGoalForm(id?)`）。关键结果行 = 输入 + 来源下拉 + ✕，可动态增删（`addKrRow` / 行内 `this.parentNode.remove()`）；支撑来源是可切换 chip；底部一条 `.gf-note` 明说「完成度和状态不用填」。
  - **维护**：卡上「···」按 **改内容 / 改时间 / 改状态** 三组列动作。状态流转带前置条件：进行中→搁置必须写复活条件、已搁置→进行中必须补期限、已完成→归档必须写 1 条复盘。详情弹窗里的「编辑」直接跳同一份表单。
  - **日常发现**：hero 的「落后于时间 2 / 缺支撑 1」两个数**本身可点**（`.metric.clickable`）→ `openTodo()` 待处理清单，3 条各写清原因 + 1 个动作按钮。汇总数不做成报表，做成入口。
- 数据模型顺手修掉一处重复：GOALS 的 `krs` 改成 `[[文本, 来源], ...]`、`links` 改成 `[[文本, 有无], ...]`，于是「缺支撑」由 `missingOf()` 从 links 里派生，删掉原来手写的 `missing` 字段（同一件事写两遍）。详情弹窗现在会标出每条关键结果的来源。
- 细节：日期输入框的默认值从 `due` 里正则取（`dateOf`），无期限的目标留空而不是编一个日期。踩过一次坑 —— 正则写成 `/\\d{4}/` 写在模板字符串的 `${}` 里会变成「字面反斜杠+d」，永不匹配且静默；把它挪到普通 JS 上下文里才对。
- 校验：jsdom 43 项断言，仅「表单字段组数」是我预期写错（实际 6 组，我按 5 组算），页面无误；CSS 花括号 843/843；标签配平（option 的差值来自 JS 生成的那几个）；`node --check` 通过；无脚本错误。

## 目标页做成完整增删改查（用户：「完整增删改查编辑的交互」）
- **前提：卡片必须数据驱动**，否则 CRUD 是假的。把 6 张静态卡 + 静态 hero/时间条/计数全部换成 `state.goals` + `render()`：`renderHero / renderStrip / renderToolbar / renderList` 四个渲染器，`activeCard / miniCard / krRow / srcChips` 四个模板。
- **派生量只算一处**：`pctOf`（完成度 = 关键结果平均达成度，每条 KR 带 `prog` 0-100）、`timePct`、`lateOf`、`handGap`、`autoSrcs`。hero 数字、时间条、筛选计数、卡片、详情弹窗全部取同一份 ⇒ 改一个数字页面各处自动一致。
- **重大简化：支撑来源不再是独立字段**，改由关键结果的「来源」去重汇总（`autoSrcs` + `srcChips`）。上一版"links 手写 + KR 各自写"两处必然打架，现在结构上不可能不一致；表单里也少一项要维护的。
- 新增/改/删/查四件都接上真交互：
  - 查：4+1 个状态筛选（chip 由 JS 渲染，「已归档」仅在真有归档项时出现）+ 2 种真排序（截止最近 / 落后最多）+ 详情弹窗 + 空状态卡（给「看全部 / 新建」出口）。
  - 增：`openGoalForm()` → `saveGoal()`，就地校验（4 处 `.gf-err` + `.invalid`，不弹 alert）→ 真插入列表 → toast 可撤销。
  - 改：卡上勾「手填」的关键结果（`toggleKr`）→ 完成度/落后/hero 立刻重算；编辑表单；顺延期限（改 due → 落后重算）；搁置（必填复活条件）；恢复（必填期限）；完成（必填复盘）；归档。
  - 删：`openDelete` 二次确认（写清"只删目标本身，不动它挂靠的资料/题库/练习"）+ 给「改为搁置」的退路 → `doDelete` → toast + **撤销**（按原 index 放回）。
- 状态机补上终点：`active / done / parked / archived`。**归档是删除的正确替代** —— 完成的复盘写完后退场但记录留着；`STATE_LABEL` 一处管理文案。
- 三个真 bug（都修了）：
  1. `saveGoal` 里用 `f.name.value` —— `form.name` 是表单自身的 name 属性，会盖掉同名输入框，保存直接抛 TypeError。改成 `f.querySelector('[name=name]')`。
  2. 日期混用：`new Date('2026-10-20')` 按 UTC 解析，与本地零点混算 ⇒ GMT+8 下"剩 25 天"变 26 天。统一走 `D(s) = new Date(s.slice(0,10) + 'T00:00:00')`。
  3. 保存回执在"只有落后变了"时会打印"完成度 65% → 65%"这种废话 ⇒ 改成收集真实变化（`完成度 X% → Y%` / `落后 33pt → 9pt`），落后文案统一走 `lateTxt()`。
- 顺手清死规则：这一页只承载目标，却带着 8 个别的分区的 CSS。用「CSS 里的类名在 HTML/JS 里是否还出现」机械筛查（要允许 JS 里写选择器 `'.plan-check'` 这种带点的形式，否则误判），删掉 57 个顶层规则块 + 3 条残留 + 1 条孤儿注释，CSS 1390 → 1258 行。**不能只按缩进抓规则**——要先做花括号配对扫描，否则会把 `@media` 里的嵌套切坏。
- 校验：jsdom **72 项断言全过、无脚本错误**（派生量算对、筛选/排序/计数/hero/时间条联动、勾 KR 后完成度 36→69 与落后 33→0、改期限后落后由 -7 变 -55、4 处必填校验拦下、新建+撤销、顺延/搁置/恢复/完成/归档五条状态流转全带前置条件、删除+撤销回原位、删空后空状态与兜底文案）；CSS 花括号 417/417、死类 0、标签配平无异常、`node --check` 通过。文件 3398 行（175KB → 171KB，但内容从静态变数据驱动）。













