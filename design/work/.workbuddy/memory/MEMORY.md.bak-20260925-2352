# 工作区记忆 · 工作模块原型

> 只留「跨页面仍然成立」的判据；单次任务的推导与实测数据写当日 `YYYY-MM-DD.md`。
> **已有技能的不再复制内容**：渲染验收 `html-ds-delivery-verify`、材质换纯色 `glass-to-solid-material-swap`、
> 查看流 `stream-view-modes`、行内增删 `inline-row-actions`、圆盘几何 `ring-nav-label-geometry`、
> 图谱 `labelled-mini-graph-svg`、同页挂两处 `cross-module-shared-view`、原型→React `prototype-to-react-port`、
> v5 导航挂载 `module-prototype-nav-mount`、等人工输入 `flow-waiting-input-step`。
> **导航 / 日志 / 部署 / 执行 / 表单弹窗 → 同目录 `MEMORY-其它页面.md`。**

## 材质与颜色：纯色范式

- **面阶四档**：页 < 区块(`--bg-subtle`) < 悬停/状态(`--bg-hover`) < 卡面(`--bg-card`)。层次 = 底色明度差 + 1px 实描边 + **两段式小阴影**（单段 `0 4px 24px` 是给毛玻璃配的，纯色下发虚）。浅 `#F4F5F7/#F7F8FA/#E9ECF1/#FFFFFF`；深 `#0A0F1C/#131A2A/#1E2840/#171F32`。**两套各自定值，只有"越浮越亮"这个方向共用。**
- **强调色三档，一档一个门槛**：`--m-main` 只做图形（3:1）；`--m-ink = color-mix(--m-main 66%, --text-primary)` 做文字（4.5:1，66% 是量出来的）；`--m-fill` 做实底，配 `--ink-on-accent`（浅白 / 深 `#0B1220`）。深色只覆写 `--m-main`，`--m-ink` 现算自动翻浅。
- **薄染混具体的面令牌，不混 `transparent`**：`color-mix(in srgb, var(--m-main) 12%, var(--bg-card))`。混 transparent 得到的仍是半透明色，结果照样取决于"叠在谁上面"。**顶栏同族：薄染混 `--bg-topbar`。**
- **描边分两档**：`--border-subtle` 只能"分隔两条相邻区域"；**输入框 / 勾选框的形只靠那条边被认出来** ⇒ `--border-field` ≥3:1（浅 `#7F899B` 3.53 / 深 `rgba(255,255,255,.36)` 3.31）。同一控件形态共用同一令牌。
- **选中态不能只靠底色差**（分段控件"白药丸压灰轨道"仅 **1.08:1** = 状态不可见）。修法不是加深边框（那变成一圈重描边），而是**给状态一条形状线索**：内侧「实心/空心」单选圆点（4.47 / 5.2，都过 3:1），两侧常驻常宽、切换不跳字。**判据：遮住颜色只留明度，还看得出选中的是哪个吗？**
- **面与面之间靠「底色差」，不靠线**：*这两块同色吗？* 同色才用线 —— 有面差再加线等于把同一条边画两遍。大片相邻色块 **1.08 已能成边**。也别用竖线分隔并排的两张卡。**模态框头/脚与正文同色 ⇒ 那两条线合法。**
- **折行区的「行间线」是必需品**：`行内行距 / 行间间距` ≥ ~0.75 时，折行的第二行会被读成"下一行"（实测 0.97）。去掉只有一条路：行间距撑到 ≥1.6 倍，代价是一屏少看 3 行 ⇒ **能折行的正文留线，单行短行（表格/列表）可以不留。**
- **文字令牌**：浅 `--text-secondary #4A5A72`(7.0) / `--text-muted #5B6A82`(5.4)；深 `#94A3B8` / `#8695AB`。`--text-muted` 压在 `--bg-hover` 上只剩 4.59:1 ⇒ 带底色的 chip（`.list-meta`/`.cmd-kbd`/`.task-tag`）一律用 `--text-secondary`。**卡片标题是"这一块的主标签"不是元数据 ⇒ secondary。** **深色下「亮底 + 白字」必然不达标**（域色 `#FB7185` 上白字 2.69:1）⇒ 翻深字；`input::placeholder` 不写颜色会吃浏览器默认灰。
- **图标一律内联 SVG，不用字符**（`▶ ✎ ↓ ⧉ 🔍 🌙 ⟳ ⌄ ✓ ⚠ 📦` 已全换）：同一 16 网格、同一 1.6 描边、`stroke="currentColor"`。理由：① 那些字符来自**四个字体族**，粗细/基线不一；② 非 ASCII 静默落到 **emoji 族**，`color` 不参与渲染 ⇒ hover/激活改色对图标无效；③ 字符撑不住容器（折叠箭头只有 11px 宽）。**路径数据只留一份**（JS 的 `ICON`）：静态标记写 `data-ico` 由 `paintIcons()` 灌入，运行时新建用 `svgIcon()`。**容器写了 width 的地方，里面的 svg 必须跟着写**，否则按基础 15px 顶破容器。
  **换成 `currentColor` 之后的三条硬约束**（都被"emoji 自带颜色"掩盖过；审计法见技能「图标体检」）：① **每个容器必须有显式 `color`** —— `<button>` 不吃继承（UA 给 `buttontext`＝黑），漏写的那个在深色下就是黑图标压深底（1.44:1）；判据是遍历**所有可见**图标，stroke 色与它**真正叠着的**不透明底色（沿祖先链找第一个 alpha>0.9）配对 ≥3:1，**别抽样**。② **缓存式图标渲染记的必须是"灌的是哪一个"**（`painted === data-ico`），不是"灌过没有" —— 后者让运行期改 `data-ico` 静默失效；运行期换图标走 `setIcon(el, name)`，别写 `innerHTML`。③ **开关类按钮的图标要跟着状态换形状**（颜色说"它正开着"，形状说"点下去会怎样"），并断言**冷启动**那一路（`?theme=dark`）—— 顺序类 bug 只在冷启动现形。
- **换材质六步 / 「纯色」清单 / 该取哪一端** → 技能 `glass-to-solid-material-swap`。只留三条判据：① 判"该不该删"看**这道渐变里有没有信息**，不看有没有 `gradient` 这个词（物体自身的形、1px 平铺纹理、遮罩 mask 可以留）；② 只改 `--bg-card` 会得到半成品 ⇒ **换材质必须连带重写阴影、重定页底色、重算 token 对比度**（换主题后深浅数值逐个相同，先查 `?theme=` 是否真生效）；删遮罩 `blur()` 后要补 α（.35 → .52）；③ 批量改档位后**把误伤的描边逐条回正**。**这些文件 untracked，大改前先 `cp`。**

## 导航壳（顶栏一级胶囊 + 侧栏圆盘/图标列 + 超级菜单 + ⌘K）

- 来源 `design/index/导航-v5.html`，**整套照搬**（顶栏与侧栏是两件，少搬一件窄屏就够不到二级）。步骤 / 名称冲突 / 几何三件套 / jsdom 冒烟清单 / 挂完必查的四处布局 → 技能 `module-prototype-nav-mount`。**导入 v5 只截「导航段」**，`.card/.section/.list-item/.btn` 一律不进 —— 那是名称冲突的根源。
- **三个跨页面口径**（与 v5 的刻意偏差）：① 圆盘 = 内容分区，**点非当前项只切内容，点当前项或中心门才下钻**；② 一级模块只有本模块可进，其余模块保留导航与真二级清单、点击只提示，**不改 active、不重建环**；③ **页头只放路径**（`模块 / 分区`，三级三段），模块标题在各面板内部，再放一个大标题就是同一个词说两遍。
- **壳令牌**：`--bg-float` + 薄染四档 `--m-soft/-2 / --m-line/-strong`，全写成 `color-mix(var(--m-main) N%, var(--bg-float))` —— 底衬写死为 `--bg-float`（壳只有这一种底衬，结果才确定）⇒ **单模块挂载要删掉 v5 的 `applyColors()`**（它运行时覆写 `--m-soft`，与这套打架）。
- **三级要么真、要么不承诺**：6 个分区配了真子视图，**过滤条件从已渲染 DOM 现算**（`.task-tag` / 组标题 / `.doc-meta` 首段 / `.client-tag`），不另写一份配置；用 `el.hidden` 过滤、不复制行；没有 `data-sub` 的元素 = 与任何子视图无关 ⇒ 永远可见。
- **`.avatar` 是顶栏用户头像**（模块的联系人头像已改名 `.contact-avatar`）。**现状**：`work_index.html` = 纯色 + v5 导航 + 12 分区（未 commit）；`.bak-glass` = 最初的毛玻璃版备份；`.smoke-nav.cjs` = 39 条交互断言（`NODE_PATH=<node workspace>/node_modules node .smoke-nav.cjs`）。

## 信息架构（跨页面）

- **同一份数据的「多种查看方式」先拆正交维度，再决定控件**。判据：**这两个选项能同时成立吗？** 能 ⇒ 两个独立控件；不能 ⇒ 一个分段控件；都不对 ⇒ 才是 tab。细节 → 技能 `stream-view-modes`。
- **「前端不管怎么取，但必须管取什么 / 取了什么 / 还没取什么」**：判据 —— 答案是否需要**同时**知道"服务端有什么"与"手上有什么"？需要就是前端的事，答案就是两者的**差**（「已载入 20 / 34 行」后端给不了，它不知道窗口开多大）。
- **原型里模拟"取数"不是为了模拟后端，是为了把"数据没取全"这个状态展示出来**：不模拟的代价是评审时看不见边界，等接了真实后端才第一次遇到"数据只来了一部分时界面长什么样" —— 那时改的是布局，不是补一句话。
- **一条横带 / 一条列表项只回答一个问题**。新增 UI 先问"它回答哪一个问题"，而不是"哪里还有空位"。**同一行里混入多种性质的信息（环境 / 位置 / 统计 / 状态）会让真正要看的那条被平均掉 ⇒ 先分性质再排，组距 > 项距。**
- **能外移的元数据就外移**：窄栏（≤260px）塞四块必密密麻麻；路径的区分度在文件名，重复前缀归 `title` 与详情栏。
- **分组不只是分类，它是缩写的许可证**：组已承担"这是谁的"，项名里的软件前缀就是冗余。**计数分两级**：组头给汇总 + 项级小红点（用 `::after` 行内点，不占列宽）。
- **分组列表的表示已统一**（日志页 / 部署页同一套）：组框白实底 + inset 描边，组头 = 色条 + 组名 + `N 条` + 折叠箭头，项缩进 12px，折叠切 `grid` 的 `0fr`。**分组维度从数据现算、不落字段**，改完数据自动归位。
- **「归类」字段（标签 / 分组 / 目录）给选项、不给自由文本；标签即分组名、不另存清单；
  能推导就替用户填但只在"没动过"时** → 技能 `inline-row-actions` §五·五。
  一句话判据：*自由文本会立刻长出「Nginx / nginx / NgInx」三个组。*
- **一个没有标签的值 = "这是什么"** ⇒ 先分类再写清范围：身份类中性底 + 灰标签 + 深值；状态类用语义色底衬，且**标签里必须带范围**（「本机」），否则它和"当前这一份"的汇总裁看起来是一件事。
- **`margin-left: auto` 的前提是"一行放得下"**：一旦折行，它会把最后一项推到第二行最右、左侧空出大洞（实测 717px，像没加载出来）。判据是**这一行会不会折**。
- **主体内容色阶不能低于元数据**：主角用 `--text-primary`，元数据才用 secondary/muted。
- **彩色徽标不要铺满整列**（密集列表里连成色带）⇒ `justify-self: start` + 胶囊；**状态码只给异常着色** —— 高亮的意义来自"少"。
- **界面能自证的，不要再配一句话解释** —— 用户先后删过**四处**同类文字（卡片名后的说明、弹窗副标题、目录选择器底部的使用说明、执行按钮旁那句「这条配置全自动…点开始就跑完」），共性都是"替 UI 做注解"。解释性文字只放 `?` 气泡这类**按需展开**的位置。
- **状态行只说"现在正在发生什么"，不说"接下来会怎样"**：默认态整行收起，执行期才报进度（`第 3 / 6 步 · 上传到 …`）。好处是**行高由按钮决定**，不再需要 `min-height`。
- **低频动作用无框文字按钮，hover 才浮出**。**长驻状态要常驻可见**，且状态切换必须挂在**同一个开关点**上。
- **某块"不好看"，先怀疑内容量**（6 行日志占不满一屏，再调间距也救不回来）。

## 页面骨架

- 项目详情各 Tab 统一 `hero-mini`（← 返回 + CORE + 项目名 + 页级动作）→ `tabs-wrap`(sticky) → 内容。兄弟页：`work_project_detail_document.html`、`工作-项目-项目详情-运维-index.html`。
- **待决策**：文档页 Tab 是页内切换面板、运维页是跳转/占位 ⇒ 统一前先定 Tab 是「页内分区」还是「跨页导航」。

## 布局与内容量（跨页面）

- **横跨整页的第一条带，左右沿必须等于「侧栏左沿 / 正文右沿」⇒ 顶栏不写 `max-width`。** 写死 1400px 独立居中时 1440 错位 4px、**1920 错位 244px**。
- **页头两行**：① 大标题 + 页级动作（同行 `space-between`）② 面包屑 + 副标题。原三行 86px → 69px。
- **面包屑只给「上级」，末段交给大标题。** 面包屑挂在标题**下方**时，末段与标题同词读起来是"重复"而非"路径 → 当前位置"（后者要求面包屑在标题**上方**）。**`crumb` 的 `cursor` 要跟着走**：只有可点的才 `pointer`。
- **区块标题不要复用页面大标题**（同一个词一屏两次）⇒ 改成**条数计数**（须写在列表生成之后）。
- **主内容 ≥1280px 拆「主列 + 侧轨」**（`minmax(0,1.7fr) minmax(0,1fr)` + `align-items: start`）。理由不是好看：列表行横跨 1076px 而文字只占 78px、侧栏槽区又空 51~74% ⇒ 次要信息挪右侧，行宽回到 ~650px。
- **内容轴收口用 `min(上限, 100% − 2×留白)`，不写死 `max-width`。** 写死 1360px 只在视口 ≥1696 才生效 ⇒ 1440/1600 下是**死代码**。**判据：先算这条规则的生效阈值。**
- **"行内稀疏"是内容问题，不是宽度问题**：留白从 0 加到 96px，标题占行宽只从 11.9% → 14.5%，而页面级多花 192px。
- **纵向填充先怀疑内容量，再调间距**（卡片内高 782、内容到 613 ⇒ 底部固定空 247px，与视口无关）。`scrollHeight == clientHeight` 只说明"不滚动"，不等于"填满了"。
- **已知项数用 `repeat(N, minmax(0,1fr))`**：`auto-fill` 会让长内容列被撑宽、宽屏多塞一列、末位落单。改成 `repeat(N)` 后**窄屏必须显式收口**。

## 踩过的坑（可复用）

1. **`[hidden]` 会被 author 的 `display` 覆盖** ⇒ 全局 `[hidden]{display:none!important}`。
2. **折叠动画别写死 `max-height`** ⇒ `display:grid; grid-template-rows: 1fr/0fr` + 子元素 `overflow:hidden;min-height:0`。
3. **`position:fixed` 子元素受祖先层叠上下文限制** ⇒ 遮罩放进同一个层叠上下文。
4. **不要用「逐块正则替换」改多块同构结构**：`[\s\S]*?` 会从**前面的块**跨过来匹配、吞掉标签 ⇒ **整块重建，或从后往前处理**；改完做两道结构自检（开/闭标签配平 + 逐块行数）。**只跑截图看不出来。**
5. **别用「按钮下标」对齐「内容下标」** ⇒ 两侧带 `data-*` 按值匹配 + `syncXxxButtons()`。
6. **grid 容器不写 `grid-template-rows` 会被子项撑破**；**空态量不出来，内容变多才暴露** ⇒ 显式 `minmax(0,1fr)`；**几何回归必须覆盖"内容最多 / 状态最靠后"的那一档**。
7. **只验证"有没有某个 class"查不出状态错**，必须逐条打印每个元素的 `class / 时间列 / 圆点`。
8. **量错对象**（完整失败模式表 → 技能 `html-ds-delivery-verify`）：① 无头截图配 `--virtual-time-budget` 时入场动画停在起始帧（起始态 `opacity:0` ⇒ 弹窗整个不可见），截图前注入 `*{animation:none!important;transition:none!important}`；② 「元素在视口外」不一定有溢出告警（横向滚动容器里 `scrollWidth-clientWidth` 恒为 0、`rect` 也正常）⇒ 按"在最近滚动祖先的可视区内"判，状态变化只改 `container.scrollLeft`；③ 测"点击后的状态"先用 `data-has-children` 过滤掉会下钻的项；④ 判"浮层可见"别取第一个同类元素，按 hovered 项取；⑤ 量几何前确认目标所在 scope **可见**（`display:none` 容器里 rect 全是 0）。
9. **媒体查询里的覆盖写在基规则之前会静默失效**（同特异度后者胜出）⇒ 修法是**提高特异度**，不是挪位置；验证只有一条：**读 computed 值**。
10. **CDP 探针报 `ECONNREFUSED 127.0.0.1:<port>` 先看端口是不是上一个实例的**（`PROFILE` 指错目录会去连已关闭的端口）。
11. **改 HTML/CSS 的三类"静默自伤"，页面都看不出来**：
    ① **切两刀删 CSS** ⇒ 中间成**孤儿声明**（`font-size:…;}` 没了选择器），浏览器丢弃、渲染正常。判据：去掉注释后算花括号深度，**depth 0 处出现 `a-b: v;` 形状的行 = 孤儿**。
    ② **`const` 的 TDZ**：函数**在定义处就被调用**、用到的 `const` 在后面 ⇒ 从那一行起**全不执行**。判据：探针第一条断言必须是「首屏无 `Runtime.exceptionThrown`」；谁先被用到，定义就放谁前面。
    ③ **对象字面量里的"兄弟"看不见彼此的 `let/const`**（`render(){let v}` / `onConfirm(){v}` ⇒ ReferenceError）：写时不报错，点"创建"才炸、表单还关掉了（像"保存成功但没生效"）⇒ **共享状态声明在字面量之外**；新建/保存的探针必须断言**数量真的 +1**。
12. **批量换令牌时 `color:` 是 `border-color:`/`outline-color:` 的子串**：一句 `replace('color: var(--x)')` 会连带把**图形档**拖进**文字档**（只深一点点，肉眼与截图都看不出）⇒ 收尾反查 `(border|outline|background)-color: var(--…-ink)` 逐条回正。同源：`--m-fill` ⊂ `--m-main`、`--bg-card` ⊂ `--bg-card-hover`。
13. **同一份数据的「写」与「读」类型必须对称**：写入存 JSON 文本、读取只认数组 ⇒ 新建后编辑读回是空、保存即清空。**类型统一在写入侧解决。**
14. **同一能力的第二份实现要抽公共部分**：同一套目录选择器在两页各一份，差异只在"选中的落点"与"找不到时的回退" ⇒ 再要第三个入口就该抽"共用目录树 + 可配置的选中语义"。
15. **结构插入"差一行"会把新内容嵌进上一个元素**：`lines[i:i]=block` 的 `i` 若指的是收口 `</div>` **那一行本身**，必须写 `i+1`。后果**看起来完全正常**（照样数得出 34 条），只有间接信号暴露（"命中数多了一处"）⇒ 插入后断言"没有嵌套的同类元素"。
16. **"方向"类错误在列表里是静默的**：新的在上 ⇒「最近 N 行」= DOM **前** N 行。写成后 N 行 ⇒ 行数对得上、时间列也在走，只是**给你看的是最旧的那批** ⇒ 断言载入那批的**首个/末个**值，不只断行数。
17. **手改过文件又从备份重放补丁 = 那处手改被静默回滚**：手动 `Edit` 要**同时写回补丁脚本**，否则下次 `cp 备份 && python patch.py` 悄悄抹掉（重放后探针只表现为"数字不同"，极易误判成别的问题）。
18. **两个控件等高别写死 px**：写死的那侧在对方字体/内边距一变就又错开 ⇒ `align-self: stretch` 跟着"这一行最高的那个"走。实测 32 vs 33 → stretch 后 33=33。
19. **顶栏里推进去的"胶囊"要跟按钮同高**（都写 30px）：折行后第一行高度由最高的那个决定，不写就两边看着不齐。

20. **`getComputedStyle` 的颜色有第二种形态**：`color-mix()` 的计算值是
    **`color(srgb 0.89 0.96 0.94)`（分量 0~1）**，不是 `rgb()`。探针按 `/\d+/g` 抓数字当 0~255 用
    ⇒ 读成"接近全黑"；只匹配 `rgba?()` 的找底色逻辑还会**跳过它**去拿更亮的祖先 ⇒ 两条路都是**假通过**
    （实测同一对颜色：错解析 3.81 / 正确 4.94）。修法与片段见技能 `html-ds-delivery-verify`「图标体检」，
    并断言"审了多少类"—— 数量对不上就说明有元素没被审到。

## 验收口径 → 技能 `html-ds-delivery-verify`

- 渲染级验收一律**先加载该技能**（覆盖清单 / 对比度实测 / 多档几何 / 命中区 / 动态底色）。本文件不复制它的内容。
- 只留两条本机事实：无头 Chrome 要 `--headless=old --no-sandbox` 且包 `timeout`；真鼠标探针走 CDP（`--remote-debugging-port=0` 读 `DevToolsActivePort`，连 `/json/list` 既有目标）。
  **`--window-size` 给的是窗口尺寸**（900 的窗口 → `innerHeight` 757）⇒ 起来后必须显式 `Emulation.setDeviceMetricsOverride`，否则以 `@media (max-height:…)` 收节奏的样式会**一直生效**（数字都量得出、结论全错）。自检：`innerHeight === 请求高度`。
