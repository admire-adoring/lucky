# MEMORY — 本工作区长期约定

作用域：`/Users/yangliu/Craft/riding-fancy/lucky-y/client/lucky/design/project`

## 🔴 强制规则（用户 2026-09-26 明确要求，长期有效，无需重复声明）

在本工作区的**所有后续对话与任务**中永久生效：

1. **不做截图 / 可视化验证** —— 不截图、不做浏览器渲染或像素级校验、不生成任何"验证用"截图或视觉对比产物。UI 改动改完即止。
2. **不加额外说明** —— 不写未请求的流程性旁白（"我做了什么、为什么这么做"）、不加未请求的总结/建议/可选方案/后续步骤清单。只给交付物本身，外加一行必要的结果定位信息。
3. **不写、不跑测试** —— 不编写测试代码（单元 / 集成 / e2e / 断言脚本），不运行任何测试、lint、类型检查或校验命令来"证明"结果。

**判定**：出现上述任一行为即视为该次任务不合格。
**解除条件**：仅当用户在**那一条消息里显式点名要求**时临时解除对应条目，且只对该次生效 —— 不改变本长期约定。

> 注：规则 2 的"额外说明"仅指用户未请求的旁白与附加内容，不含交付物内部必要的内容表达。
